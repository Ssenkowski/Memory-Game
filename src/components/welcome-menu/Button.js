import React from 'react';

export const Button = () =>  (
      <div>
          <button
            // onClick={play}
            className="play-button"
            id="Play" 
            > Play </button>
      </div>
    );
    
    export default Button;